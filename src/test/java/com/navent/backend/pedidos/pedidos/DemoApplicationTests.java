package com.navent.backend.pedidos.pedidos;


import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DemoApplicationTests {

	@Test
	void contextLoads() {
	}

	@Test
	void insertPedido(){
		/** 
		PedidoBuilder pedidoBuilder = new PedidoBuilder();

		Pedido pedido = pedidoBuilder.withIPedido(154)
		.withNombre("Pan")
		.withMonto(10.15)
		.withDescuento(1.7f)
		.build();
		*/
	}

}
